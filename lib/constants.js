// Fixed for every artist console — never make this artist-configurable.
export const AGENCY_KICKER = 'VCCP Media Cultural Intelligence';

// The console's own product name — independent of artistConfig.wordmark,
// which names the artist the console is currently skinned for.
export const STUDIO_NAME = 'The Recording Studio';
